"""
1. Create an array with 5 values and delete the value at index 2 without using the built-in `del` function. 
2.WAP to reverse an array without using the built-in `reverse()` function.
"""
import numpy as np
x=np.array([10, 20, 30, 40, 50])
print("Original array:", x)
